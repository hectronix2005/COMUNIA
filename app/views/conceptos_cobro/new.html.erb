<% content_for(:title, "Nuevo Concepto - #{@logia.nombre}") %>

<div class="mb-4">
  <h1><i class="bi bi-plus-lg me-2"></i>Nuevo Concepto de Cobro</h1>
  <p class="text-muted"><%= @logia.nombre %> (<%= @logia.codigo %>)</p>
</div>

<div class="card">
  <div class="card-body">
    <%= render "form" %>
  </div>
</div>
