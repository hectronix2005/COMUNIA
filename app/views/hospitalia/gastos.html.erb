<% content_for(:title, "Gastos – Hospitalia") %>

<div class="container py-4">
  <div class="d-flex align-items-center gap-2 mb-4">
    <%= link_to hospitalia_path, class: "btn btn-sm btn-outline-secondary" do %>
      <i class="bi bi-arrow-left"></i>
    <% end %>
    <h4 class="mb-0 fw-bold"><i class="bi bi-arrow-up-circle text-danger me-2"></i>Gastos</h4>
  </div>

  <div class="row g-4">
    <% if controller.send(:puede_gestionar?) %>
      <div class="col-12 col-lg-4">
        <div class="card border-0 shadow-sm">
          <div class="card-header bg-transparent fw-semibold">Registrar Gasto</div>
          <div class="card-body">
            <%= form_with model: @gasto, url: hospitalia_create_gasto_path, method: :post do |f| %>
              <% if @gasto.errors.any? %>
                <div class="alert alert-danger py-2 small">
                  <% @gasto.errors.full_messages.each do |m| %><div><%= m %></div><% end %>
                </div>
              <% end %>
              <div class="mb-3">
                <%= f.label :concepto, "Concepto *", class: "form-label fw-semibold small" %>
                <%= f.text_field :concepto, class: "form-control form-control-sm", required: true %>
              </div>
              <div class="mb-3">
                <%= f.label :monto, "Monto ($) *", class: "form-label fw-semibold small" %>
                <%= f.number_field :monto, class: "form-control form-control-sm", step: "0.01", min: "0.01", required: true %>
              </div>
              <div class="mb-3">
                <%= f.label :fecha, "Fecha *", class: "form-label fw-semibold small" %>
                <%= f.date_field :fecha, class: "form-control form-control-sm", required: true %>
              </div>
              <div class="mb-3">
                <%= f.label :beneficiario_id, "Beneficiario (opcional)", class: "form-label fw-semibold small" %>
                <%= f.select :beneficiario_id,
                      @miembros.map { |m| [m.user.nombre_completo, m.id] },
                      { include_blank: "Sin especificar" },
                      class: "form-select form-select-sm" %>
              </div>
              <div class="mb-3">
                <%= f.label :descripcion, "Descripción", class: "form-label fw-semibold small" %>
                <%= f.text_area :descripcion, rows: 2, class: "form-control form-control-sm" %>
              </div>
              <%= f.submit "Registrar", class: "btn btn-danger btn-sm w-100" %>
            <% end %>
          </div>
        </div>
      </div>
    <% end %>

    <div class="col">
      <% if @gastos.empty? %>
        <div class="text-center text-muted py-5">
          <i class="bi bi-arrow-up-circle display-4 opacity-25"></i>
          <p class="mt-3">No hay gastos registrados.</p>
        </div>
      <% else %>
        <div class="card border-0 shadow-sm">
          <div class="table-responsive">
            <table class="table table-hover align-middle mb-0">
              <thead class="table-light">
                <tr>
                  <th>Fecha</th>
                  <th>Concepto</th>
                  <th>Beneficiario</th>
                  <th class="text-end">Monto</th>
                  <% if controller.send(:puede_gestionar?) %><th></th><% end %>
                </tr>
              </thead>
              <tbody>
                <% @gastos.each do |g| %>
                  <tr>
                    <td class="text-muted small"><%= g.fecha.strftime("%d/%m/%Y") %></td>
                    <td>
                      <div class="fw-semibold small"><%= g.concepto %></div>
                      <% if g.descripcion.present? %><div class="text-muted" style="font-size:.72rem;"><%= g.descripcion %></div><% end %>
                    </td>
                    <td class="small"><%= g.beneficiario&.user&.nombre_completo || "–" %></td>
                    <td class="text-end fw-semibold text-danger small">-<%= number_to_currency(g.monto, unit: "$", delimiter: ".", separator: ",") %></td>
                    <% if controller.send(:puede_gestionar?) %>
                      <td>
                        <%= link_to hospitalia_destroy_gasto_path(id: g.id),
                              data: { turbo_method: :delete, turbo_confirm: "¿Eliminar este gasto?" },
                              class: "btn btn-sm btn-outline-danger" do %>
                          <i class="bi bi-trash"></i>
                        <% end %>
                      </td>
                    <% end %>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
